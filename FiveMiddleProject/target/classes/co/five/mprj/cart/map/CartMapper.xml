<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="co.five.mprj.cart.map.CartMapper">
	<!-- 장바구니 전체조회 -->
	<select id="cartSelectList"
		resultType="co.five.mprj.cart.service.CartVO">
		SELECT p.PRODUCT_THUMBNAIL_URL,
		p.PRODUCT_NAME,
		p.PRODUCT_EXPLAIN,
		p.PRODUCT_PRICE,
		c.MEMBER_ID,
		c.CART_NUM,
		p.PRODUCT_NUM
		FROM PRODUCT p JOIN CART c
		ON p.PRODUCT_NUM = c.PRODUCT_NUM
		WHERE c.MEMBER_ID = #{memberId}
	</select>

	<!-- 장바구니에 등록 -->
	<insert id="cartInsert"
		parameterType="co.five.mprj.cart.service.CartVO">
		<selectKey keyProperty="CartNum" resultType="int"
			order="BEFORE">
			SELECT
			CASE WHEN MAX(CART_NUM) IS NULL 
			THEN 1 ELSE MAX(CART_NUM) + 1 
			END AS CARTNUM
			FROM CART
		</selectKey>
		INSERT INTO CART
		VALUES(#{cartNum}, #{productNum},
		#{productName},#{productPrice},
		#{productDate}, #{productThumbnailUrl}, #{memberId})
	</insert>

	<!-- 장바구니 삭제 -->
	<delete id="cartDelete"
		parameterType="co.five.mprj.cart.service.CartVO">
		DELETE FROM CART WHERE CART_NUM = #{cartNum}
	</delete>
</mapper>